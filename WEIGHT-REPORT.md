# Аудит веса сайта — Github Link2PDF

Дата: 2026-08-29 · Замер: `vite build` (gzip-размеры считает Vite; на хостинге с Brotli будет ещё меньше).

## Короткий ответ

**Нет, сайт не тяжёлый.** Первая загрузка — около **40 КБ (gzip)** вместе со шрифтами.
Внутри всего ~115 КБ «сырого» кода, и он весь сжимается до ~35 КБ. Единственный
по-настоящему крупный файл — jsPDF (~130 КБ gzip), но он грузится **только в момент
нажатия «Export PDF»** и не влияет на открытие страницы.

## Что весит (текущее состояние, после оптимизаций)

| Файл | Размер | gzip | Когда грузится |
| --- | --- | --- | --- |
| `index.html` | 4.7 КБ | ~1.7 КБ | сразу |
| `styles.css` (30 дизайнов + @font-face) | 47.1 КБ | 8.6 КБ | сразу |
| `main.ts` (весь JS) | 70.6 КБ | 24.7 КБ | сразу |
| favicon `filetype-txt.svg` | 0.5 КБ | 0.4 КБ | сразу |
| Шрифты woff2 (Inter 400/500/600, Merriweather 400/700/900) | 36 файлов, 713 КБ в `dist` | — | только нужные подмножества (~150–200 КБ типично) |
| **Итого первая загрузка** | **~115 КБ** | **~40 КБ** | |
| `jspdf` | 398.7 КБ | 129.2 КБ | **только при экспорте PDF** |

Как это работает:
- 36 woff2-файлов лежат в `dist/assets`, но браузер по `unicode-range` скачивает
  только те подмножества, символы которых реально встречаются на странице
  (обычно latin + cyrillic = ~6–10 файлов ≈ 150–200 КБ).
- Все файлы хэшированные → после первого визита браузер берёт их из кэша,
  повторные загрузки почти ничего не весят.

## Уже сделано (без каких-либо изменений внешнего вида)

1. **Шрифты Google перенесены в репозиторий** (self-hosted через `@fontsource`).
   - Убраны `<link>` на `fonts.googleapis.com` и `fonts.gstatic.com` — больше нет
     внешних запросов, страница не зависит от доступности Google.
   - Внешний вид идентичен: те же файлы шрифтов, те же `font-family: 'Inter' /
     'Merriweather'`, тот же `font-display: swap`, все подмножества
     (latin, latin-ext, cyrillic, cyrillic-ext, greek, greek-ext, vietnamese)
     с теми же `unicode-range`.
   - В бандл не попадают устаревшие woff-фолбэки (плагин в `vite.config.ts`
     отрезает их) — только woff2.
   - Лицензии SIL OFL сохранены в `THIRD_PARTY_NOTICES.md`.

2. **Объединены print-стили.** Встроенный блок `@media print` в `index.html`
   (~1.6 КБ) дублировал и перекрывал такой же блок в `styles.css`. Правила
   объединены в один блок в `styles.css`, дубликат удалён из HTML.
   - `index.html`: 7.7 КБ → 4.7 КБ.
   - Печать выходит байт-в-байт так же: каскад и `!important`-приоритеты сохранены.

Проверки: тесты 102/102, `tsc` без ошибок, `vite build` чисто, все 36 woff2
отдаются сервером (200).

## Ещё можно (по убыванию эффекта)

### 1. Асинхронная загрузка CSS шрифтов — первая отрисовка быстрее
Сейчас CSS (включая @font-face) блокирует первую отрисовку: пока файл не скачан,
страница белая. Текст появляется сразу после загрузки CSS — это уже быстрее, чем
было с Google (один домен, HTTP/2, без DNS/TLS к Google). Дальше можно убрать
шрифтовой CSS из render-blocking цепочки (`media="print" onload="this.media='all'"`),
тогда текст появляется мгновенно, а шрифты применяются через ~0.3–1 с.
Цена: при самом первом визите возможен краткий «мельк» системного шрифта.

### 2. Ленивая загрузка ru/ko словарей — ~2 КБ gzip из JS
`translations.ts` (8 КБ) со всеми тремя языками попадает в основной бандл.
Грузить `ru`/`ko` динамически при переключении языка — основной JS похудеет
на ~5 КБ (2 КБ gzip). Внешний вид не меняется.

### 3. Проверить сжатие и кэш на хостинге
- Brotli/gzip на сервере (если хостинг не сжимает — цифры выше удвоятся).
- `dist/assets/*` (хэшированные) отдавать с `Cache-Control: max-age=31536000, immutable`.

### 4. Минификация HTML — ~0.5–1 КБ
Vite не минифицирует `index.html`. Плагин уберёт отступы/комментарии.

### 5. Предзагрузка jsPDF при наведении на «Export PDF»
Не уменьшает вес, но экспорт начинается мгновенно (`import('jspdf')` по
`mouseenter`/`focus` на кнопке).

## Вывод

Сайт лёгкий и устроен правильно: ~40 КБ gzip первая загрузка, тяжёлые зависимости
отложены, внешние сервисы больше не нужны. Дальнейшее уменьшение — косметическое
(пункты 1–5 выше).
