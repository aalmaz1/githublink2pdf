# Аудит веса сайта — Github Link2PDF

Дата: 2026-08-29 · Замер: `vite build` (gzip-размеры считает Vite; на хостинге с Brotli будет ещё меньше).

## Короткий ответ

**Нет, сайт не тяжёлый.** Первая отрисовка теперь ждёт только ~32 КБ (gzip), а
единственный крупный файл — jsPDF (~130 КБ gzip) — грузится **только в момент
нажатия «Export PDF»** (и даже раньше — при наведении на кнопку).

## Что весит (текущее состояние, после всех оптимизаций)

### Первая загрузка (критический путь)

| Файл | Размер | gzip | Когда грузится |
| --- | --- | --- | --- |
| `index.html` (минифицирован) | 3.7 КБ | ~1.3 КБ | сразу |
| `styles.css` (30 дизайнов) | 36.9 КБ | 7.6 КБ | сразу (рендер-блокирующий) |
| `main.ts` (весь JS) | 66.8 КБ | 22.8 КБ | сразу |
| favicon `filetype-txt.svg` | 0.5 КБ | 0.4 КБ | сразу |
| **Итого первая отрисовка** | **~108 КБ** | **~32 КБ** | |

### Загружается после первой отрисовки (не блокирует)

| Файл | Размер | gzip | Когда грузится |
| --- | --- | --- | --- |
| 6 чанков CSS шрифтов (@font-face, 36 правил) | ~2 КБ каждый | ~0.7 КБ | сразу после первой отрисовки (requestIdleCallback) |
| woff2 шрифтов (36 файлов в dist) | 713 КБ всего | — | только нужные подмножества (latin+cyrillic ≈ 150–200 КБ), потом кэш |
| `ru.js` | 2.8 КБ | 1.3 КБ | только при первом переключении на русский |
| `ko.js` | 2.2 КБ | 1.3 КБ | только при первом переключении на корейский |
| `jspdf` | 398.7 КБ | 129.2 КБ | при наведении/фокусе на «Export PDF» + при самом экспорте |

## Что сделано (внешний вид не изменился)

1. **Шрифты Google перенесены в репозиторий** (self-hosted через `@fontsource`).
   Убраны `<link>` на `fonts.googleapis.com`/`fonts.gstatic.com` — ноль внешних
   запросов. Внешний вид идентичен: те же файлы, имена, `unicode-range`,
   `font-display: swap`. Лицензии OFL — в `THIRD_PARTY_NOTICES.md`.

2. **Шрифты больше не блокируют первую отрисовку.** CSS шрифтов грузится
   отдельными чанками сразу после первого кадра (`requestIdleCallback`):
   - первая отрисовка ждёт только UI-стили (36.9 КБ вместо 49 КБ);
   - woff2-файлы браузер скачивает сам по `unicode-range`, по мере надобности;
   - `font-display: swap` — текст мгновенно виден системным шрифтом и плавно
     подменяется на Inter/Merriweather, как только они пришли.
   *Почему не через `<link media="print">`: Vite не собирает `@import` во
   входном CSS-файле (эмитит его сырым), а JS-динамический импорт — надёжный
   путь, который собирается правильно.*

3. **Ленивые словари ru/ko.** Русский и корейский переехали в отдельные чанки
   (`ru.js` 2.8 КБ, `ko.js` 2.2 КБ), грузятся только при первом переключении
   языка. Английский (основной) остаётся в бандле. Быстрое переключение
   языков защищено от гонок: применяется только последний запрос.
   При первом переключении возможна задержка в миллисекунды на загрузку чанка;
   дальше он в кэше. Фолбэк — английский, если чанк не загрузился.

4. **HTML минифицируется** (плагин `html-minifier-terser` в `vite.config.ts`):
   отступы и комментарии убраны — `index.html` 4.7 → 3.7 КБ.

5. **Предзагрузка jsPDF при наведении/фокусе** на «Export PDF» — экспорт
   начинается мгновенно; неудачная предзагрузка безвредна (повторная попытка
   при клике).

6. **Объединены print-стили** (из прошлого этапа): дублирующий блок `@media print`
   убран из HTML — печать работает так же, HTML легче.

Также: из бандла исключены устаревшие `.woff`-фолбэки (плагин чистит финальный
CSS и удаляет `.woff`-ассеты — в dist только 36 woff2).

## Ещё можно

- **Проверить сжатие и кэш на хостинге**: Brotli/gzip (если хостинг не сжимает —
  цифры выше удвоятся); `dist/assets/*` (хэшированные) с
  `Cache-Control: max-age=31536000, immutable`.
- **Preload первых woff2** (`<link rel="preload" as="font">` для latin-400) —
  начнут качаться чуть раньше. Эффект небольшой, но бесплатный.

## Проверки

- Тесты: 102/102; `tsc --noEmit` — чисто.
- Сборка: 6 чанков CSS шрифтов (36 @font-face), 0 woff, 36 woff2, все 200 OK
  на предпросмотре; dev-режим работает.
