# Аудит веса сайта — Github Link2PDF

Дата: 2026-08-29 · Замер: `vite build` (gzip-размеры считает Vite; на хостинге с Brotli будет ещё меньше).

## Короткий ответ

**Нет, сайт не тяжёлый.** Первая загрузка — около **35 КБ (gzip)**, это уровень лёгкой
статической страницы. Внутри всего ~115 КБ «сырого» кода, и он весь сжимается до ~35 КБ.
Единственный заметный вес — **шрифты Google (~150–250 КБ)** и **jsPDF (~130 КБ gzip)**, но
jsPDF грузится только в момент нажатия «Export PDF».

## Что весит (после выполненной оптимизации)

| Файл | Размер | gzip | Когда грузится |
| --- | --- | --- | --- |
| `index.html` | 5.0 КБ | 1.7 КБ | сразу (первая загрузка) |
| `styles.css` (30 дизайнов) | 36.9 КБ | 7.6 КБ | сразу (первая загрузка) |
| `main.ts` (весь JS) | 70.6 КБ | 24.7 КБ | сразу (первая загрузка) |
| favicon `filetype-txt.svg` | 0.5 КБ | 0.4 КБ | сразу |
| **Итого первая загрузка** | **~113 КБ** | **~34.6 КБ** | |
| `jspdf` | 398.7 КБ | 129.2 КБ | **только при экспорте PDF** |
| Google Fonts (Inter 400/500/600, Merriweather 400/700/900) | ~150–250 КБ | — | внешний ресурс, кэшируется браузером после первого визита |

Нагрузка распределена правильно: главный модуль лёгкий, тяжёлые зависимости
(jsPDF) отложены. CSS на 100% используется (проверено скриптом — мёртвых правил нет),
все 6 начертаний шрифтов реально задействованы в дизайнах.

## Уже сделано (без каких-либо изменений внешнего вида)

1. **Объединены print-стили.** В `index.html` был встроенный блок `@media print`
   (~1.6 КБ), который дублировал и перекрывал такой же блок в `styles.css`.
   Правила объединены в один блок в `styles.css` без потери ни одного свойства,
   дубликат удалён из HTML.
   - `index.html`: 7.7 КБ → 5.0 КБ (gzip 2.3 → 1.7)
   - Печать выходит байт-в-байт так же: каскад правил сохранён, `!important`-приоритеты
     те же, тесты 102/102 проходят.

## Предложения по дальнейшему уменьшению (по убыванию эффекта)

### 1. Асинхронная загрузка Google Fonts — экономия ~150–250 КБ с критического пути ⭐
Сейчас `<link rel="stylesheet" href="fonts.googleapis.com/...">` **блокирует первую
отрисовку**: пока браузер не скачает CSS шрифтов, страница белая.
Достаточно поменять атрибуты на `media="print" onload="this.media='all'"` — и текст
появляется сразу, а шрифты догружаются и применяются через ~0.3–1 с.
**Внешний вид после загрузки — идентичный.** Единственное отличие: при самом первом
визите текст на долю секунды может отрисоваться системным шрифтом (затем плавно
подменится). На повторных визитах шрифты в кэше — подмены не видно.
*Требует решения: допустим ли краткий «мельк» шрифта при первом визите.*

### 2. Ленивая загрузка русского и корейского словарей — ~2 КБ gzip из JS
Сейчас `translations.ts` (8 КБ) со всеми тремя языками попадает в основной бандл,
хотя большинству пользователей нужен только английский. Если грузить `ru`/`ko`
динамически при переключении языка — основной JS похудеет на ~5 КБ (2 КБ gzip).
Внешний вид не меняется (словари те же), но код переключения языка чуть сложнее.

### 3. Проверить сжатие и кэш на хостинге
- Включить **Brotli/gzip** на сервере (если хостинг не сжимает — цифры выше удвоятся).
- Убедиться, что `dist/assets/*` (хэшированные файлы) отдаются с `Cache-Control: max-age=31536000, immutable` — тогда повторные визиты почти ничего не грузят.
- Для GitHub Pages это уже так по умолчанию.

### 4. Минификация HTML — ~0.5–1 КБ
Vite не минифицирует `index.html`. Плагин вроде `vite-plugin-html-minifier` уберёт
отступы/комментарии. Эффект небольшой, но бесплатный.

### 5. Предзагрузка jsPDF при наведении на «Export PDF» — не вес, но скорость
Вес не уменьшится, зато экспорт начнётся мгновенно: при `mouseenter`/`focus` на кнопке
запускаем `import('jspdf')` заранее. Для пользователя — чистое улучшение отклика.

### 6. (Не рекомендуется из-за внешнего вида) убрать Google Fonts совсем
Максимальное снижение (~200 КБ и минус внешний запрос), но дизайны с serif-заголовками
(Merriweather) и часть типографики заметно изменятся — это уже нарушает ваше условие
«пользователь не заметил».

## Вывод

Сайт уже лёгкий и устроен правильно. Единственный по-настоящему крупный внешний
ресурс — шрифты Google, и с ним есть ровно один «невидимый» способ работать дальше —
асинхронная загрузка (пункт 1). Всё остальное — экономия в пределах 0.5–2 КБ gzip.
